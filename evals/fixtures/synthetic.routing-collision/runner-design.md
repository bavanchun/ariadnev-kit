# Runner scenario request

Generate concrete crash, retry, corruption, approval-expiry, and duplicate-effect
scenarios for a local event-sourced runner. For each scenario, state severity,
precondition, injected fault, invariant, expected events, and proof layer.

This task is scenario generation. Do not conduct web research, debate whether
the architecture should proceed, or implement the runner.
