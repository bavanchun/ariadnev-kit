# Skill-routing fixture

This frozen corpus supplies neutral local context for lightweight trigger and
output-contract checks. The user request, not the fixture name, determines the
selected skill. No network state, credentials, or provider-specific files exist.
