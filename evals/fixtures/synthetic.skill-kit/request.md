# Benchmark triage skill request

Create a reusable skill that classifies behavioral benchmark failures by outcome,
routing, safety, artifact proof, trajectory, and each performance measurement.
It must define positive and negative triggers, required inputs, deterministic
workflow, output schema, privacy rules, and tests for collision behavior.
