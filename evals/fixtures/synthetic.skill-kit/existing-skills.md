# Existing neighboring skills

- `vc:test`: executes and classifies tests; does not repair behavior
- `vc:code-review`: reviews completed changes; does not implement fixes
- `vc:fix`: diagnoses and repairs a proven defect
- `vc:scenario`: generates risk and edge-case matrices

The new benchmark-triage skill must not duplicate these boundaries.
