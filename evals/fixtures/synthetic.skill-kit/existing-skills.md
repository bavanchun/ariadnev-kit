# Existing neighboring skills

- `av:test`: executes and classifies tests; does not repair behavior
- `av:code-review`: reviews completed changes; does not implement fixes
- `av:fix`: diagnoses and repairs a proven defect
- `av:scenario`: generates risk and edge-case matrices

The new benchmark-triage skill must not duplicate these boundaries.
