# Evaluation routing fixture

`src/eval-router.ts` owns tier selection. `src/eval-command.ts` only translates
CLI options into a routing request. Answer read-only questions with relative
file and symbol evidence; do not modify this corpus.
