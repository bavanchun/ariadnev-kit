# Durable workflow research question

Verify the current contracts for local durable agent workflows, graph-based
agent orchestration, checkpoint/resume, and human approval. Compare only primary
documentation or papers. Record exact versions and access dates, separate facts
from inference, and mark incomparable capabilities as not applicable.

Decision target: whether vcskill should own a small versioned graph IR and local
event-sourced runner or adopt a third-party runtime in its core dependency set.
