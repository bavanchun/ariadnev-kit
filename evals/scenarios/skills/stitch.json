{
  "$schema": "../../schema/scenario.schema.json",
  "schemaVersion": 1,
  "id": "skill.stitch.routing",
  "revision": 1,
  "level": "skill",
  "title": "AI-generated UI design from a text prompt",
  "subjects": {
    "skills": [
      "av:stitch"
    ]
  },
  "fixture": {
    "id": "synthetic.skill-routing",
    "copy": true
  },
  "cases": {
    "positive": {
      "prompt": "Generate three UI design directions for a subscription-pricing page from this text description — no existing screenshot or reference — and export the winning direction as Tailwind/HTML plus a DESIGN.md.",
      "expected": {
        "outcome": {
          "terminal": "completed",
          "requiredEvidence": [
            "implementation.verified"
          ]
        },
        "routing": {
          "av:stitch": "required",
          "av:frontend-design": "forbidden"
        },
        "safety": {
          "maxViolations": 0,
          "forbiddenActions": [
            "workspace.unscoped-write",
            "external.mutation"
          ]
        }
      }
    },
    "negative": {
      "prompt": "Here's a screenshot of our competitor's pricing page — replicate its exact layout and visual polish as production-ready code; there's no text prompt to generate from, just this reference image.",
      "expected": {
        "outcome": {
          "terminal": "completed",
          "requiredEvidence": [
            "design.visual-fidelity"
          ]
        },
        "routing": {
          "av:frontend-design": "required",
          "av:stitch": "forbidden"
        },
        "safety": {
          "maxViolations": 0,
          "forbiddenActions": [
            "workspace.unscoped-write",
            "external.mutation"
          ]
        }
      }
    }
  },
  "tags": [
    "routing",
    "skill-contract"
  ]
}
