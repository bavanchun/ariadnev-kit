# Port report — test skill content parity

Worktree branch rebased to `c57173f` (tip of the content-parity branch) before
editing: the assigned base commit still carried the pre-ultra 138-line
SKILL.md, while the coordinator's brief referenced the 161-line version with
the `## Ultra Verifier Mode` section. No local commits existed, so the reset
was a fast-forward.

## Per-item disposition

| # | Item | Disposition | Notes |
|---|------|-------------|-------|
| 1a | `references/create-suite-workflow.md` | Ported | Verbatim except `ak:scout` → `av:scout` (2 sites incl. prose) |
| 1b | `references/optimize-suite-workflow.md` | Ported | Verbatim except `ak:scout` → `av:scout` |
| 1c | `references/audit-suite-workflow.md` | Ported | Verbatim except `ak:scout` → `av:scout` |
| 1d | SKILL.md workflow sections 4–6 with "Load when" lines | Ported | Reworded only for brand (`av:scout`) |
| 1e | Intent routing: `create`/`optimize`/`audit` rows in the Default table | Ported | Verbatim |
| 1f | argument-hint routing | Ported | Merged upstream's hint with av's existing `[--ultra]`: `create\|optimize\|audit [scope] [--advice] [--ultra] [--interview]` |
| 2a | `## Flags (create / optimize / audit)` — `--advice`, `--ultra`, `--interview` | Ported | Verbatim; `--interview` uses av's `ask_user capability` placeholder, same as upstream |
| 2b | `## Advisory supervision (--advice)` | Reworded | Upstream inlines the supervisor/invocation text; av's convention links `../av-cook/references/advisory-supervision.md` (verified present) and lists only this skill's three checkpoints (post-scout go/no-go, pre-apply change list, when stuck) + the never-bypass line. Mirrors the skill-creator precedent from the prior port commit |
| 2c | Other upstream-only flags | None found | Upstream SKILL.md documents only `--advice`, `--ultra`, `--interview`; nothing else to port |
| 3 | Re-key `## Ultra Verifier Mode (--ultra)` | Done | Now names the three reference files; keeps `../av-brainstorm/references/ultra-verifier-mode.md` pointer, the plain-execution → stop rule, and gains upstream's "composes with `--advice` and `--interview`" line. Bullet labels re-keyed to `create`/`optimize`/`audit` |
| 4a | Diff `report-format.md` | No delta | Byte-identical to upstream |
| 4b | Diff `test-execution-workflow.md` | No delta | Byte-identical to upstream |
| 4c | Diff `ui-testing-workflow.md` | Cut (1 line) | Only deltas are brand strings plus upstream's `agent-browser screenshot -o profile.png`; av's own agent-browser skill documents `agent-browser screenshot <path>` (no `-o`), so av's form kept |
| — | `metadata.version` | Bumped 1.0.0 → 1.1.0 | Matches upstream's 1.1.0 and the version-bump precedent of the prior port commit |
| — | Frontmatter `workflow.precedes` metadata (upstream) | Cut | av carries this in the required `## Workflow position` section instead |
| — | Editorial visual layer, `--no-antv`, `visual.*` prefs | Out of scope | Per brief |

## Line counts after edit

| File | Lines | Gate |
|------|-------|------|
| SKILL.md | 218 | ≤ 300 OK |
| references/create-suite-workflow.md | 60 | ≤ 800 OK |
| references/optimize-suite-workflow.md | 61 | ≤ 800 OK |
| references/audit-suite-workflow.md | 70 | ≤ 800 OK |
| references/report-format.md | 58 | unchanged |
| references/test-execution-workflow.md | 103 | unchanged |
| references/ui-testing-workflow.md | 64 | unchanged |

## Verification

| Check | Result |
|-------|--------|
| `av validate --strict` | all checks passed (105 skills, 16 agents, 14 hooks); warning count unchanged at 131 |
| Brand grep over `kit/skills/test/` | no upstream product name, `ak-`, `ak:`, or `ak ` forms |
| Every `references/*.md` linked from SKILL.md | yes — sections 1–6 + Ultra section |
| Cross-skill links | `../av-cook/references/advisory-supervision.md`, `../av-brainstorm/references/ultra-verifier-mode.md` — both shape-valid and existing |
| Phantom `av <sub>` commands | none introduced (no `av` invocations added) |
| description ≤ 200 chars, trigger verb | unchanged frontmatter, already compliant |

## Unresolved questions

- The worktree was spawned on a base (941d570) behind the branch the brief
  described; I reset to `c57173f`. If the coordinator expected the older base,
  the commit still merges cleanly into the content-parity branch (it is a
  descendant), but flag it if another base was intended.
