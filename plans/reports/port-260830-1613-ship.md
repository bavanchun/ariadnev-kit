# Port report: `ship` 2.1 → 2.3 (upstream `ak-ship` 2.3.0)

Branch: `worktree-agent-a796931a7041ba359`. Scope: `kit/skills/ship/**` only.
Verification run: `rg`, `wc -l`, `diff`, `av validate --strict` (105 skills,
all checks passed, 131 warnings — identical count to the pre-edit baseline,
none naming `ship`). No test suite, build, or benchmark was run.

## Gaps ported

| Gap | Upstream source | Where it landed | Lines before → after |
|---|---|---|---|
| Channel aliases `stable\|main` → official, `dev\|next` → beta; unknown-token stop | `ak-ship/SKILL.md` Arguments + Ship Mode Detection; `references/ship-workflow.md` Step 1.2 | `SKILL.md` Arguments table, Ship Mode Detection, Quick Start; `references/ship-workflow.md` Step 1.2 | SKILL.md 183 → 254 |
| `--both` dual-target ship | `ak-ship/SKILL.md` "Dual-target ship"; `references/dual-stage-workflow.md` | `SKILL.md` "Dual-target ship (`--both`)"; new `references/dual-stage-workflow.md` (adds a paragraph tying it to `av:vibe --both`) | — → 90 |
| `--merge` reviewed-merge handoff | `ak-ship/SKILL.md` `--merge` row, Step 13, Important Rules; `references/review-and-merge-workflow.md` | `SKILL.md` `--merge` row, Pipeline Step 13, "Reviewed merge delegation" / "Fail closed" rules; new `references/review-and-merge-workflow.md`; `ship-workflow.md` Step 13 loader | — → 31 |
| `--advice` kongming supervision | `ak-ship/SKILL.md` "Advisory supervision"; `ship-workflow.md` Step 1.6-1.7 + "Mandatory advice checkpoint after Steps 4-5" | `SKILL.md` "Advisory supervision (`--advice`)" linking `../av-cook/references/advisory-supervision.md`; `ship-workflow.md` Step 1.6-1.7 and the post-Steps-4-5 checkpoint | ship-workflow.md 391 → 302 |
| Step 13 becomes review+merge, social moves to Step 14 | `ak-ship/SKILL.md` Pipeline; `ship-workflow.md` Steps 13-14 | `SKILL.md` Pipeline block, `--social` section, Output format; `ship-workflow.md` Steps 13-14 | (counted above) |
| `references/release-and-social-workflow.md` (Steps 6-9 + Step 14) | `ak-ship/references/release-and-social-workflow.md` | new file, but its body is ariadnev's own Steps 6-9 and social step moved out of `ship-workflow.md` (keeps av's journal opt-out contract verbatim) | — → 152 |
| Output format: Advice / Review / Merge / CI lines | `ak-ship/SKILL.md` Output Format | `SKILL.md` Output format (+ pointer to the two-stage lines) | (counted above) |
| Frontmatter: description, keywords, argument-hint, version 2.3.0 | `ak-ship/SKILL.md` frontmatter | `SKILL.md` frontmatter (description 193 chars; `origin: ported`, `author: upstream` kept) | (counted above) |

Unchanged: `references/auto-detect.md` (103, byte-identical to upstream),
`references/pr-template.md` (87, differs from upstream only by the existing av
hook paths). `scripts/compose-build-in-public.cjs`: two header-comment lines now
point at `references/release-and-social-workflow.md` (the step moved); no code
change.

Line totals after: SKILL.md 254 (limit 300); every reference ≤ 152 (limit 800).

## Every `av` / `review-pr` flag referenced, with proof

| Reference | Proof it exists |
|---|---|
| `av:review-pr <PR> --fix --reply --merge --advice` | `kit/skills/review-pr/SKILL.md` line 8 (`argument-hint`) and lines 56-62 (Modes: Fix loop, Reply, Merge, Advice; "Flags compose") |
| `av:git merge-pr` (named as what review-pr's merge mode activates) | `kit/skills/git/SKILL.md` lines 7-8, 27, 49 |
| `kongming` subagent | `kit/agents/kongming.md` |
| `av journal create <title> --component <name> --status Resolved --body <text> --json` | `~/.local/bin/av journal create --help` (av 1.3.0): options are exactly `--component`, `--status`, `--body`, `--json` |
| `--json` envelope `.data.path` for the journal file | `packages/cli/src/cli/journal-command.ts` line 76 (`envelope("journal.create", { file, path, dryRun })`), `packages/cli/src/cli/json-envelope.ts` (`{ schema_version, kind, data }`) |
| `av plan resolve / show / check <n> / status [status] / close` (Step 9b, untouched) | `~/.local/bin/av plan --help` lists all five |
| `av:vibe --both` contract (beta stage, then stable only after beta CI green; promotion vs direct-PR; unrelated-work stop) | `kit/skills/vibe/SKILL.md` lines 43, 177-192, 222-229 — the ported `dual-stage-workflow.md` gates match it |
| Links `../av-cook/references/advisory-supervision.md`, `../../av-cook/references/plan-state-files-first.md`, `../av-review-pr/references/{writing-language,pr-body-contract}.md` | all present under `kit/skills/{cook,review-pr}/references/` |
| `kit/skills/journal/scripts/post-social.cjs`, `kit/skills/ship/scripts/compose-build-in-public.cjs` fallbacks | both present |

`review-pr` already ships `--merge` in ariadnev, so the handoff is described with
the exact upstream flag set; no gap there.

## Defects fixed while porting (pre-existing in av's social step)

- `COMPOSE_BIN` source-repo fallback pointed at `kits/engineer/skills/av-ship/…`,
  a path that does not exist here → `kit/skills/ship/scripts/compose-build-in-public.cjs`.
- `av journal create … --summary … --stdin < file` used two flags the CLI has
  never had. The invocation lint missed it because the `|` inside the `$(…)`
  title expression cuts the argv before the flags. Rewritten to the real
  surface (`--component ship --status Resolved --body "$BODY" --json`) and
  `JOURNAL_PATH` is now actually assigned (it was read by the next step but
  never set).

## Dropped or deliberately not ported

- Upstream's model-routing parenthetical in the advisory section (Claude → Fable
  5; Codex → `gpt-5.6-sol`; Cursor → `claude-fable-5-high`) and its "host
  detection" pointer. They live in upstream's
  `ak-brainstorm/references/advisory-supervision.md`; ariadnev's shared protocol
  at `kit/skills/cook/references/advisory-supervision.md` has no host/model
  section, and the brief said link, don't duplicate. Gap noted below.
- Upstream Step 8's `ak config prefs resolve --json | jq … .prefs.journal.auto`
  — av's journal opt-out contract (journal skill's `scripts/resolve-config.cjs`)
  kept verbatim as instructed.
- Upstream Step 12b `ak plan update --linked-pr <n>` — av's `plan update` takes
  `<phase> <status>` and has no `--linked-pr`; av's "no CLI stores this" Step 12b
  kept.
- Upstream's `kits/core/...` link and hook paths — replaced by the av paths
  already in the file.
- `## Quality gates` (absent upstream) kept — it is a lint-required section.

## Frozen retrieval corpus

`evals/context/corpus-manifest.json` line 82 lists `kit/skills/ship/SKILL.md`,
so this port changes a corpus member. The benchmark was **not** run; the
coordinator regenerates it.

## Unresolved questions

1. Step 14 now tags the social journal entry `--component ship`. Upstream had
   no component (it had `--summary`). Keep, or leave `--component` off?
2. av's shared `advisory-supervision.md` (cook-owned) carries no host detection
   / model routing. Port that section there in a cook pass, or accept that
   ariadnev routes `kongming` purely through the agent definition?
3. `av:vibe --both` orchestrates the two stages itself with `/av:ship beta` then
   `/av:ship official`. Now that ship honors `--both --merge` directly, should
   vibe's step 7/10 delegate to it? Out of this port's scope (vibe is owned
   elsewhere).
4. The report is committed alongside the skill change so it survives worktree
   cleanup (`plans/reports/` is tracked, 40 files). Split it out if a pure
   kit-only commit is preferred.
