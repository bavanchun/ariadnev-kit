# Port: `av:plan` content parity — debate mode, real CLI surface, stub reconciliation

Date: 2026-08-30 · Branch: `worktree-agent-a185cc803ac093d0c` · Source: upstream plan skill (SKILL.md + references)

Base note: the assigned worktree branched from `941d570`, which predates the
`--ultra` port. The ultra work is committed on the current integration branch
(commit `4269716`, tip `c57173f`); this branch was reset onto `c57173f` before
editing so the debate port builds on the ultra port instead of forking it, and
it merges fast-forward into that integration branch.

## Per-gap outcome

| # | Gap | What happened |
|---|-----|---------------|
| 1 | `--debate` mode | Ported. `argument-hint` + mode-table row + flowchart edge + steps 5-7 + red-team omit list in `SKILL.md`; auto-detection note, Mode Exclusivity extension (`--fast` + `--debate` now the canonical contradiction, per upstream), full `## Debate Mode` section (11 steps: evidence packet, scaffold-first, read pass, dispatch override, usable-candidate gate, pointer re-assert, `## Debate Synthesis` with resolution order, gates, hydration, reminder) inserted before `## Ultra Mode` in `references/workflow-modes.md`; hydration/availability/cook-reminder rows added |
| 2 | False CLI claim | Fixed. "there is no scaffolding subcommand" removed from `SKILL.md` and `plan-organization.md`; subcommand list now the full 20 from `av plan --help` (`use show create add-phase kanban parse validate migrate list resolve update check uncheck status close phase search reindex archive cleanup`); "run `--help` before any mutation, never copy syntax into plans" rule added; files-first + "reindex reports, nothing to rebuild" kept |
| 3 | Write guard + read pass | Ported as one `SKILL.md` rule bullet (**Generated-file write guard — mandatory read pass**), adapted to the proven stub shape: `create` writes only `plan.md`, each `add-phase` one phase stub; enumerate + read all before the first long write. It absorbs the old "read before overwriting" rule (noted inside the bullet) instead of duplicating it |
| 4 | Post-create flow | Reconciled to one sequence: scaffold (`create --use`, `add-phase` per phase) → read pass → fill → `use` only if `--use` was not passed. All three `av plan use` sites updated (`SKILL.md` Rules + step 5 + quality gate; `workflow-modes.md` Pre-Creation Check + Ultra step 2; `plan-organization.md` steps 1/4 + Active Plan State) |
| 5 | Canonical phase template | Not duplicated — av already had it in `SKILL.md`. Moved to `plan-organization.md` → new `### Canonical Phase File Template` (frontmatter matches the `add-phase` stub byte-for-byte: `phase title status priority effort dependencies`), 2-line pointer left in `SKILL.md`; freed the room the debate/CLI additions needed (SKILL.md ends at 299) |
| 6 | Cross-plan dependencies | Not ported — `output-standards.md` already carries the full detection section with `global:`/`project:` prefixes. `av plan validate` proved to accept and preserve the keys (below), so the frontmatter form stays. Fixed the false claim "the live plan CLI's status operation is authoritative for resolved dependency state" (`plan-organization.md`): no subcommand reads `blockedBy`/`blocks`; resolution is the agent's, via `av plan list` + frontmatter. Same note added under the detection section |

Cut/reworded from upstream debate text: dual-pointer machinery
(`set-active-plan.cjs` + `ak plan use` — av has one branch pointer),
`status: todo` (→ `pending`), the internal "plan.md Design Decision #2"
citation, the "table cell kept short for column width" aside, and the
upstream reports-path-resolver rationale (replaced with the av-true reason:
the plan dir must exist and be the branch's active plan before dispatch).
`SKILL.md` `version` bumped 1.4.0 → 1.5.0 (new mode flag; matches upstream's
own bump; no test pins it).

## Proven `av plan` facts relied on

| Fact | Evidence (exact `--help` / run) |
|---|---|
| `create` exists | `create [options] <title>` — "Bootstrap a new plan directory from the template"; options `--description <text>`, `--priority <p>` (P1\|P2\|P3), `--use` ("also point this branch at the new plan"), `--json` |
| `add-phase` exists | `add-phase [options] <title>` — "Append a new phase-NN-<slug>.md to a plan, and a row in its table"; options `--plan <name>`, `--depends <list>`, `--json` |
| Full subcommand list | `av plan --help` lists exactly the 20 commands now in SKILL.md |
| `create` writes only `plan.md` | Scratch run: one file; stub = frontmatter (`title description status priority created:` yymmdd) + `## Outcome`, 5-col `## Phases` table (`# | Phase | Dependencies | Effort | Status`), `## Open questions` (template: `packages/cli/src/plan/plan-scaffold.ts`) |
| `add-phase` stub | frontmatter `phase title status priority effort dependencies`; sections Overview / Related Code Files / Implementation Steps / Success Criteria / Risk Assessment; row appended, `--depends 1` → `dependencies: [1]` |
| `--use` sets the branch pointer | `create --use` → `"selected": true`; `av plan resolve` exits 0 with the dir (exit 1, "nothing selected", before) |
| `validate` semantics | "Check one plan's directory format; exits 1 when it is invalid"; findings on a planted bad phase: `status "todo" is not one of pending, in-progress, completed, cancelled` and `phase 2 is also declared by phase-02-second-phase.md` (`checkPlanIntegrity`, `plan-mutations.ts:107`) |
| `blockedBy`/`blocks` accepted | Added `blockedBy: [global:…]` / `blocks: [project:…]` to the scratch `plan.md`; `validate` stayed `valid: true` (only the planted phase findings); no CLI read of the keys (`grep blockedBy packages/cli/src` → none) |
| `update` vs the 5-col stub table | `av plan update 1 in-progress` rewrote the row's last cell to `**in-progress**` and the phase file's `status:` together |
| `reindex` | "Re-read every plan and report what is malformed (there is no index to rebuild)" — confirmed against the broken phase |

## Line counts after edit

| File | Lines | Cap |
|---|---|---|
| `kit/skills/plan/SKILL.md` | 299 | 300 |
| `references/workflow-modes.md` | 427 | 800 |
| `references/plan-organization.md` | 327 | 800 |
| `references/output-standards.md` | 246 | 800 |

## Gates

- `av validate --strict`: 105 skills, 16 agents, 14 hooks — all checks passed;
  131 warnings, identical to the pre-edit baseline.
- Brand scan of added diff lines (`ak[-: ]`, product names, `localhost:3456`,
  `plans.db`): zero hits.
- Only the four files above changed; no test runner, no build, `evals/`
  untouched.

## Unresolved questions

1. `kit/skills/cook/references/plan-state-files-first.md` still asserts
   "There is no `create` and no `add-phase` … Do not invent `av plan create`"
   — now false, and shared by every plan-consuming skill. Out of this task's
   one-skill scope; the branch `fix/plan-state-doc-matches-cli` may be the
   owner. Needs a follow-up before the two files contradict in an install.
2. `SKILL.md` is a frozen retrieval-corpus member and was edited — the
   coordinator owes the benchmark regeneration (per task brief).
3. Ultra Mode step 2 was minimally retargeted at Debate Mode step 2 so the two
   scaffolding instructions cannot drift; flagged since `--ultra` was
   nominally out of scope.
