{
  "name": "vcskill-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "pnpm --filter vcskill build",
    "test": "vitest run && node --test \"kit/hooks/**/*.test.cjs\" && node --test \"packages/cli/scripts/**/*.test.mjs\"",
    "test:hooks": "node --test \"kit/hooks/**/*.test.cjs\"",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "lint": "tsc -p packages/cli/tsconfig.json --noEmit",
    "build:release": "pnpm --filter vcskill build:release",
    "docs-bundle:generate": "pnpm --filter vcskill docs-bundle:generate",
    "test:build-binaries": "pnpm --filter vcskill test:build-binaries",
    "docs-bundle:verify-web-lock": "pnpm --filter vcskill verify:web-consumer-lock",
    "changeset": "changeset",
    "version-packages": "changeset version"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@vitest/coverage-v8": "^2.1.8",
    "ajv": "^8.20.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
